# v0.1.18 - UI Language Switch and i18n Foundation

> 设计批次：F076 / Enhancement / High
> 目标版本：F076 -> v0.1.48；F077/F078/F080 作为 F076 验收切片合并；F079 进入 locale watchlist（Display Language MVP 已拆出为 F104 / v0.1.20）
> 状态：Planned
> Rebaseline：v0.1.18 已发布，本文保留 F076-F080 的原始设计；用户可见的中/英显示语言 MVP 前移到 [F104 / v0.1.20](v0.1.20.md#feature_104-display-language-mvp)。当前由 F076 在 v0.1.48 统一承接 typed locale、pseudo-locale、CI guard 和 display/response language 边界；F079 仅在两套 locale 完整且出现明确需求后重开。
> Source of truth：本文档 / [FEATURE_LIST.md](../FEATURE_LIST.md)
> 调研日期：2026-06-18

---

## FEATURE_076: UI Language Switch and i18n Foundation

## 背景

客户反馈 KodaX-Space 没有中文/英文显示切换。这个反馈成立，而且不应该只用一个「中文 / English」按钮解决。当前 app 已经存在大量硬编码 UI 文案：全局菜单、Settings、左右侧栏、popout 面板、权限弹窗、toast、命令面板、HelpOverlay、workflow 面板等都直接写死英文或中英混排。越晚处理，后续回填成本越高。

本 feature 的核心结论：

**做一个全局 i18n 基础设施，并把语言切换作为用户偏好接入；第一版覆盖全局菜单和高频 UI 面板，不改变用户内容、模型输出和工具输出。**

2026-06-22 更新：为了让 v0.1.20 先具备可用的中/英切换，F104 从本文拆出一个 Display Language MVP。本文后续仍作为完整 i18n foundation 的设计源，重点从「先做第一个可见切换」调整为「把 F104 的基础设施扩成全量 typed locale 和质量门禁」。

## 外部调研摘要

| 来源 / 产品                                                                                                | 可借鉴实践                                                                                                                                                                     | 对 KodaX-Space 的取舍                                                                                      |
| ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| [VS Code Display Language](https://code.visualstudio.com/docs/configure/locales)                           | 默认英文；检测 OS UI 语言；可通过 Configure Display Language 显式覆盖；语言包缺失时安装；切换后必要时提示 restart；偏好写入用户配置。                                          | 采用「默认跟随系统 + 用户显式覆盖 + 持久化」模型；renderer 立即切换，native menu 如有平台限制才提示重启。  |
| [Android per-app language preferences](https://developer.android.com/guide/topics/resources/app-languages) | 多语言用户可能希望某个 app 使用不同于系统的语言；Android 把 per-app language 做成一等偏好，并建议 in-app picker 与系统设置一致。                                               | 语言是 app 级用户偏好，不是临时 UI toggle；入口放在 Preferences，而不是常驻标题栏。                        |
| [Electron app locale APIs](https://www.electronjs.org/docs/latest/api/app#appgetpreferredsystemlanguages)  | Electron 区分 application locale、system locale、preferred system languages；`getPreferredSystemLanguages()` 适合决定 UI 语言，`getSystemLocale()` 更适合日期/数字等区域格式。 | UI 文案用 preferred languages；日期、数字、相对时间等格式化用 effective locale 的 `Intl`。                 |
| [W3C Internationalization Quick Tips](https://www.w3.org/International/quicktips/)                         | 应提供清晰入口让用户访问自己偏好的语言版本；语言入口应使用目标语言；使用 UTF-8；避免从多个碎片拼句子。                                                                         | Preferences 中显示 `简体中文` / `English`；locale 文件保持 UTF-8；文案用完整句子 key 或 message function。 |
| [i18next fallback guidance](https://www.i18next.com/principles/fallback)                                   | locale variant 可以回退到更宽泛语言；缺 key 时显示 key 可行但不推荐，会增加维护成本。                                                                                          | `zh-CN`、`zh-Hans`、generic `zh` 统一到 `zh-CN`；MVP 只支持 `zh-CN` / `en-US`；测试中缺 key 直接失败。     |
| [FormatJS / React Intl](https://formatjs.github.io/docs/react-intl/)                                       | React app 常用 provider + hook，并基于 `Intl` 做日期、数字、相对时间格式化。                                                                                                   | 增加 `I18nProvider` 和格式化 helper；MVP 不引入运行时依赖，直接用平台 `Intl`。                             |
| [Mozilla Fluent Syntax Guide](https://projectfluent.org/fluent/guide/)                                     | 自然语言需要处理变量、复数、性别、变位等复杂表达。                                                                                                                             | MVP 先用 typed message + message function；复数/复杂语法明显增多后再评估 Fluent/ICU。                      |

## 产品决策

F076 只提供 3 个用户可见语言模式：

| Mode     | 含义                 | effective locale                                                      |
| -------- | -------------------- | --------------------------------------------------------------------- |
| `system` | 跟随系统首选 UI 语言 | 系统首选语言含简体中文或 generic Chinese 时为 `zh-CN`，否则为 `en-US` |
| `zh-CN`  | 强制简体中文 UI      | `zh-CN`                                                               |
| `en-US`  | 强制英文 UI          | `en-US`                                                               |

默认值：`system`。

入口：`Settings > Preferences > Language`。不在标题栏、侧栏、底栏常驻一个语言切换按钮。命令面板可以加 `Preferences: Change Language`，但它只打开 Preferences，不做第二套切换入口。

切换行为：

- renderer UI 立即更新；
- main 进程重建 Electron native menu；
- app-owned menu label 必须跟随语言；
- Electron/OS role 自带 label 如果平台不支持即时刷新，可以提示「重启后完整生效」，但这只能是兜底。

重要边界：

- UI language 不等于 assistant response language；
- 不自动改变模型回复语言；
- 不翻译用户消息、assistant 消息、tool stdout/stderr、文件路径、代码、Markdown 正文、model/provider/tool 名称；
- 原始 SDK 错误可以用本地化外壳包一层，但 raw message 本身不强行翻译。

## 目标

1. 建立 main 和 renderer 共用的 typed i18n 基础设施。
2. 增加 `system` / `zh-CN` / `en-US` 语言偏好。
3. 从 stored preference + OS preferred languages 解析 effective locale。
4. 覆盖全局菜单、Settings、左右侧栏、popout 面板、弹窗、toast、命令面板、HelpOverlay、workflow 面板等高频 UI。
5. 把语言偏好持久化到 `~/.kodax/space/settings.json`，并迁移现有 settings schema。
6. 增加测试和 CI guard，避免新 UI 文案绕过 locale。
7. MVP 零运行时新依赖，保持现有 Electron + React + Zustand 架构。

## 非目标

- 不做机器翻译。
- 不做云端 language pack marketplace。
- 不做繁体中文、日文、韩文、企业术语表。
- 不翻译 LLM 回复、用户内容、tool 输出、文件内容、artifact 正文、代码块、文件路径、model/provider/tool 名。
- 不在 MVP 引入完整 ICU/Fluent parser。
- 不做常驻 app chrome 语言切换器。

## 用户故事

1. 中文系统用户首次打开 Space，菜单、设置、面板标题、按钮、空状态默认显示简体中文。
2. 英文系统上的中文用户可以在 Preferences 改成简体中文，UI 立即生效，重启后仍保持。
3. 中文系统上的英文用户可以强制 English，重启后仍保持。
4. `system` 模式下，用户改变 OS 首选语言并重启 Space，Space 会按新系统语言解析 UI 语言。
5. 开发者新增 UI 文案时有 TypeScript key autocomplete；某个 locale 缺 key 时测试失败。

## UX 方案

### Preferences 入口

在现有 `Settings > Preferences` 中新增 `Language` 区块，位置建议放在 Workspace 之前：

```text
Language
Menus, panels, dialogs, and app controls.

(*) Follow system    Current: Simplified Chinese
( ) 简体中文
( ) English
```

中文 UI 下：

```text
语言
用于菜单、面板、弹窗和应用控件的显示语言。

(*) 跟随系统    当前：简体中文
( ) 简体中文
( ) English
```

交互规则：

- 只给 3 个选项；
- 选择后立即保存；
- 成功后显示本地化的 inline success；
- 保存失败则回滚到旧语言并显示本地化错误；
- `简体中文` 和 `English` 使用目标语言名称，保证用户在当前 UI 语言不熟悉时也能找到自己的语言。

### Native Menu

当前 native menu 很小：macOS app menu、Edit、View、Window。F076 只翻译 app-owned label，保留 Electron role。

英文标签：

- Edit
- View
- Window
- Reload
- Force Reload
- Toggle Developer Tools
- Toggle Full Screen

中文标签：

- 编辑
- 视图
- 窗口
- 重新加载
- 强制重新加载
- 切换开发者工具
- 切换全屏

注意：

- macOS role-owned 系统项可继续交给 Electron/OS；
- 不能为了翻译破坏 Cmd+C/V/A/Z 等 role 快捷键；
- Windows/Linux 虽然 `autoHideMenuBar` 为 true，Alt 菜单仍应保持正确 label。

### Renderer 覆盖范围

F076 第一版覆盖这些 UI surface：

| Surface                    | 示例                                                                         |
| -------------------------- | ---------------------------------------------------------------------------- |
| Settings                   | tabs、section title、description、button、validation、provider settings copy |
| LeftSidebar                | Projects、Recents、Archived、context menu、session action、empty state       |
| BottomBar                  | send state、attach action、model/effort/permission selector、slash hint      |
| RightSidebar / popouts     | Plan、Tasks、Diff、MCP、Preview、Agents.md、context panel                    |
| Conversation chrome        | system notice、retry/open-settings action、tool card label、copy status      |
| CommandPalette             | action label、hint、searchText                                               |
| HelpOverlay                | shortcut label、description                                                  |
| Toast / notification       | success、warning、error copy                                                 |
| Permission / AskUser modal | label、risk copy、confirm/cancel verb                                        |
| Workflow UI                | policy label、run control、progress/empty state                              |

低频 developer diagnostics 和 raw SDK error 可以先作为 known gap。

## 架构设计

### Shared i18n package

新增 workspace package：

```text
packages/space-i18n/
  package.json
  tsconfig.json
  src/
    index.ts
    types.ts
    resolve.ts
    translate.ts
    format.ts
    locales/
      en-US.ts
      zh-CN.ts
```

公共 API：

```ts
export type SupportedLocale = 'en-US' | 'zh-CN';
export type LanguageMode = 'system' | SupportedLocale;
export type MessageId = keyof typeof enUS;

export const SUPPORTED_LOCALES: readonly SupportedLocale[];
export const DEFAULT_LOCALE: SupportedLocale;

export function resolveLocale(input: {
  mode: LanguageMode;
  preferredLanguages: readonly string[];
}): SupportedLocale;

export function createTranslator(locale: SupportedLocale): {
  locale: SupportedLocale;
  t(id: MessageId, vars?: Record<string, string | number>): string;
  formatDate(value: Date | number, options?: Intl.DateTimeFormatOptions): string;
  formatNumber(value: number, options?: Intl.NumberFormatOptions): string;
};
```

字典示例：

```ts
export const enUS = {
  'settings.title': 'Settings',
  'settings.subtitle': 'KodaX Space controls',
  'settings.language.title': 'Language',
  'settings.language.description': 'Menus, panels, dialogs, and app controls.',
  'settings.language.system': 'Follow system',
  'settings.language.zhCN': '简体中文',
  'settings.language.enUS': 'English',
} as const;
```

变量文案优先使用完整 message 或 function：

```ts
'settings.language.systemEffective': ({ localeName }) => `Current: ${localeName}`,
```

不要拼碎片：

```ts
t('current') + ': ' + localeName;
```

### Locale resolution

解析规则：

1. `languageMode === 'zh-CN'` 时直接用 `zh-CN`。
2. `languageMode === 'en-US'` 时直接用 `en-US`。
3. `languageMode === 'system'` 时按 OS preferred languages 顺序解析。
4. 下列值映射到 `zh-CN`：
   - `zh-CN`
   - `zh-Hans`
   - `zh-Hans-*`
   - generic `zh`
5. 其它语言映射到 `en-US`。
6. `C`、`POSIX`、空字符串、未知值映射到 `en-US`。

说明：MVP 不把 `zh-Hant` 静默映射到简体中文。繁体中文是后续明确支持项，不应让繁中用户误以为已经获得完整本地化。

### Settings persistence

当前 settings：

```ts
{
  version: 1,
  defaultWorkspace: string
}
```

F076 迁移到：

```ts
{
  version: 2,
  defaultWorkspace: string,
  languageMode: 'system' | 'zh-CN' | 'en-US'
}
```

迁移规则：

- settings 文件不存在：默认 `languageMode: 'system'`。
- v1 文件：保留 `defaultWorkspace`，补 `languageMode: 'system'`，下一次写入时落成 v2。
- 非法 languageMode：fallback 到 `system`，写 warn，不阻塞启动。

IPC 更新：

```ts
settings.get -> {
  defaultWorkspace: string;
  languageMode: LanguageMode;
  effectiveLocale: SupportedLocale;
  preferredSystemLanguages: string[];
}

settings.setLanguageMode -> {
  languageMode: LanguageMode;
} -> {
  languageMode: LanguageMode;
  effectiveLocale: SupportedLocale;
}
```

`settings.setDefaultWorkspace` 保留，但返回值也升级为完整 settings payload。

### Main process

main 进程负责 persisted language preference 和 native menu。

新增：

```text
apps/desktop/electron/i18n/locale.ts
apps/desktop/electron/i18n/menu.ts
```

职责：

- `settingsStore.load()` 读取 languageMode；
- `app.getPreferredSystemLanguages()` 获取 OS preferred languages；
- 用 `resolveLocale()` 得到 effective locale；
- 创建 `createTranslator(effectiveLocale)`；
- 把 `main.ts` 里的 menu template 抽成 `buildApplicationMenu(t)`；
- 启动时设置 native menu；
- `settings.setLanguageMode` 成功后重建 native menu；
- 推送 settings changed event 给 renderer。

伪流程：

```ts
const settings = await settingsStore.load();
const effectiveLocale = resolveLocale({
  mode: settings.languageMode,
  preferredLanguages: app.getPreferredSystemLanguages(),
});
Menu.setApplicationMenu(buildApplicationMenu(createTranslator(effectiveLocale)));
```

### Renderer

新增：

```text
apps/desktop/renderer/src/i18n/I18nProvider.tsx
apps/desktop/renderer/src/i18n/useI18n.ts
apps/desktop/renderer/src/i18n/bootstrap.ts
```

启动流程：

1. 读取 `localStorage['kodax-space.languageMode']` 作为 first-paint cache；
2. 用 `navigator.languages` 本地解析 initial locale；
3. 调 `settings.get` 获取 main 权威状态；
4. 若 main 返回不同 languageMode/effectiveLocale，更新 provider；
5. 用户在 Preferences 切换语言时，调用 `settings.setLanguageMode`，成功后更新 provider 并写 localStorage cache。

`App.tsx`：

```tsx
<I18nProvider initialLocale={bootstrapLocale}>
  <AppShell />
</I18nProvider>
```

组件用法：

```tsx
const { t } = useI18n();
return <h2>{t('settings.title')}</h2>;
```

非 React 用法：

- command source builder 接收 `t`，不要在 module top-level 固化 translated label；
- popout/action registry 中的 label 需要在 render 时计算；
- dynamic action 的 `searchText` 同样本地化，保证命令面板中英都可搜。

### Message key 规范

使用语义 key，不用英文句子作为 key：

```text
settings.title
settings.preferences.tab
settings.providers.tab
sidebar.projects.title
session.menu.rename
permission.allowOnce
workflow.policy.autoStart.confirm
```

规则：

- key 是稳定契约；
- 删除 key 前必须确认所有 locale 和调用方；
- provider/model/tool/file path/env var 不进入 locale；
- dynamic error 先映射 stable error code；
- unknown error 用本地化外壳 + sanitized raw message。

### Formatting

使用 effective locale 的 `Intl` 处理：

- 日期和时间；
- 相对时间；
- 数字和百分比；
- 文件大小，如果已有 helper 则改造成 locale-aware。

保持原样：

- 文件路径；
- 命令名；
- model/provider/tool 名；
- env var；
- code / markdown / artifact body。

## 实施步骤

### Step 1: inventory + guardrail

- `rg` 扫描 main/renderer 中可见硬编码文案；
- 分成 visible UI、raw data、developer diagnostics、comments；
- 在本文或临时 tracking note 中记录覆盖清单；
- 先加 locale key parity test。

### Step 2: shared i18n package

- 新建 `packages/space-i18n`；
- 实现 `resolveLocale`、`createTranslator`、format helper；
- 添加 dictionary parity / fallback / interpolation tests；
- root build 中加入该 package，早于 renderer/main build；
- 不增加 runtime dependency。

### Step 3: settings migration + IPC

- `settings/store.ts` schema 升到 v2；
- 添加 `setLanguageMode`；
- 更新 `packages/space-ipc-schema/src/channels/settings.ts`；
- 更新 `apps/desktop/electron/ipc/settings.ts`；
- 增加 migration、invalid value、atomic write 测试。

### Step 4: renderer provider + Preferences UI

- 增加 `I18nProvider`；
- Preferences 新增 Language section；
- 先迁 SettingsModal，使该 feature 可以 self-host；
- languageMode 成功写入后同步 localStorage first-paint cache。

### Step 5: native menu

- 从 `main.ts` 抽出 menu builder；
- app-owned label 走 `t()`；
- `settings.setLanguageMode` 后重建 menu；
- 单测覆盖 `en-US` / `zh-CN` template label。

### Step 6: high-frequency UI extraction

推荐迁移顺序：

1. SettingsModal + ProviderCard + CustomProviderForm；
2. LeftSidebar + Project/Session context menu；
3. RightSidebar + popout panel title；
4. BottomBar + model/permission/agent selector + CommandToolbar；
5. Conversation system notice + copy/retry/open-settings action；
6. PermissionModal + AskUserModal；
7. HelpOverlay + CommandPalette；
8. WorkflowPolicySection + Workflow panels。

第一版以高频 UI 覆盖为准，raw SDK/dev diagnostics 留 known gap。

### Step 7: CI checks

新增检查：

- `en-US` 和 `zh-CN` key 集合完全一致；
- translation value 不能为空；
- renderer snapshot / e2e 不出现 raw message id；
- settings migration 单测；
- e2e 切语言后多个稳定 label 无需重启即更新。

可选后续：

- `scripts/check-i18n.mjs` 先做 advisory，扫描选定 UI 目录中的 JSX hard-coded text；等 F076 完成后再升级成 required CI。

## 测试计划

### Unit tests

`resolveLocale`：

- `system + ['zh-CN'] -> zh-CN`
- `system + ['zh-Hans-US'] -> zh-CN`
- `system + ['zh-Hant-TW'] -> en-US`
- `system + ['en-US'] -> en-US`
- `system + ['C'] -> en-US`
- explicit `zh-CN` / `en-US` 总是优先

Translator：

- 两个 locale key 完全一致；
- interpolation 正常；
- test/dev 中 missing key 失败。

Settings：

- v1 迁移到 v2；
- invalid file 安全 fallback；
- `setLanguageMode` 原子写入；
- `settings.get` 返回 effectiveLocale。

Menu：

- `buildApplicationMenu(tZh)` 包含中文 label；
- `buildApplicationMenu(tEn)` 包含英文 label；
- Edit role 快捷键不丢。

### E2E

- clean settings + stub `navigator.languages = ['zh-CN']`，Settings title 为中文；
- Preferences 切 English，Settings title、tab label、sidebar title、close tooltip 更新；
- 重启后 English 保持；
- 切回 Follow system，effective locale 按 system 解析；
- 960x600 / 1280x800 下中文和英文没有明显溢出/重叠。

### Manual QA

- Windows：Alt menu label、titlebar 行为；
- macOS：app menu role、Cmd+C/V/A/Z；
- 中英各跑一遍主流程：
  - 新建/打开 session；
  - 配 provider；
  - permission prompt；
  - 运行 tool；
  - 打开右侧各 popout；
  - 打开 HelpOverlay。

## 验收标准

- `Settings > Preferences > Language` 存在 3 个选项：`Follow system` / `简体中文` / `English`。
- 新安装默认 `system`。
- 中文系统默认简体中文；非中文系统默认英文。
- 手动切换后 renderer 立即更新，重启后保持。
- native menu 中 app-owned label 跟随语言。
- 高频菜单和面板已中英覆盖。
- 用户可见 UI 不显示 raw message id。
- locale key parity test 进入 CI。
- 现有 settings 文件迁移后不丢 `defaultWorkspace`。
- 用户内容、assistant 内容、tool 输出、文件路径、model/provider/tool 名保持原样。

## 风险与缓解

| 风险                            | 缓解                                                                                                   |
| ------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 半迁移导致 UI 中英混杂          | 先迁顶层 chrome 和高频路径；保留覆盖清单；新高频字符串 review 时必须走 locale。                        |
| 启动时语言闪烁                  | renderer 用 localStorage + `navigator.languages` 做 first-paint cache，再与 main `settings.get` 对齐。 |
| native menu role 被破坏         | role item 不重写行为，只翻译 app-owned label；macOS 单测/手测 copy paste select all。                  |
| 拼接句子导致中文别扭            | 使用完整句子 key 或 message function；review rule 禁止拼碎片。                                         |
| settings migration 影响用户配置 | zod union parse v1/v2 + atomic write；malformed file 单测。                                            |
| 过度翻译 raw technical content  | 明确保留 provider/model/tool/path/code/raw output 原样。                                               |

## 开放问题

1. `zh-Hant` 是否 fallback 到 English 还是 Simplified Chinese？建议：MVP fallback English，直到明确支持繁中。
2. Command Palette 是否加 `Preferences: Change Language`？建议：加，但只打开 Settings。
3. assistant response language 是否跟随 UI？建议：不在 F076 做，后续做独立 preference。
4. renderer localStorage 是否能作为 main 不可用时的真实来源？建议：不能；main settings 是 source of truth，localStorage 只做 first-paint cache。

## 后续 feature

### F077：dev-only pseudo-locale / 文案扩展 QA

> Current disposition: absorbed into F076 as an acceptance slice for `v0.1.48`.

增加只在 dev/test 可用的 pseudo-locale，用拉长字符串、加边界标记和模拟宽字符的方式暴露 UI 溢出、按钮挤压和布局抖动。该 locale 不进入用户设置，不参与正式语言列表。

验收：开发者可以通过 env/测试 flag 开启 pseudo-locale；核心 UI 在 960x600 和 1280x800 下没有明显重叠；截图回归可覆盖设置、侧栏、权限弹窗、workflow 面板。

### F078：i18n scanner 从 advisory 升级为 CI required

> Current disposition: absorbed into F076 as an acceptance slice for `v0.1.48`.

把 F076 中的硬编码文案扫描从提醒型脚本升级为 CI gate。允许 raw technical content、model/provider/tool/path/code 保持原文，但高频用户可见 UI 文案必须走 typed locale key。

验收：新增 JSX/TSX 用户可见文案绕过 locale 时 CI 失败；允许名单有注释和 owner；locale key parity test 与 scanner 一起运行。

### F079：繁体中文支持

> Current disposition: watchlist. Reopen only after the two-locale foundation is complete and user demand is demonstrated.

在 `zh-CN` / `en-US` 稳定后增加 `zh-Hant`。不使用简体中文静默 fallback 伪装完整支持；繁中必须有独立字典、fallback、排版 QA 和目标系统语言解析。

验收：`system` 模式下繁中系统可解析到 `zh-Hant`；三套 locale key 完全一致；繁中 UI 主路径无明显截断或错字。

### F080：Assistant response language preference

> Current disposition: absorbed into F076 as the acceptance boundary between display locale and assistant response preference.

增加模型回答语言偏好，并明确与 UI display language 分离。用户可以让 UI 用英文、assistant 默认中文，或反过来。该设置只影响 Space 注入的会话偏好，不翻译用户内容、tool 输出或 artifact 正文。

验收：设置项独立于 UI language；新 session 能带入回答语言偏好；已有 session 不被静默改写；Quick Ask 与 Coder/Partner 的默认策略一致且可覆盖。

## 参考

- [VS Code Display Language](https://code.visualstudio.com/docs/configure/locales)
- [Android per-app language preferences](https://developer.android.com/guide/topics/resources/app-languages)
- [Electron app locale APIs](https://www.electronjs.org/docs/latest/api/app#appgetpreferredsystemlanguages)
- [W3C Internationalization Quick Tips](https://www.w3.org/International/quicktips/)
- [i18next fallback guidance](https://www.i18next.com/principles/fallback)
- [FormatJS / React Intl](https://formatjs.github.io/docs/react-intl/)
- [Mozilla Fluent Syntax Guide](https://projectfluent.org/fluent/guide/)
