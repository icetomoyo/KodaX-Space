# v0.1.12 — Artifact 层 + 近场首杀 skill

> 2026-07-09 completion update: F049 is completed as a Partner Workbench built-in code-knowledge pack, using the existing Partner conversation/session path, F109 generated office artifacts, and F113 reviewed file proposals. It does not add a new execution IPC or give Partner raw `write`/`edit`/`bash`.
> 2026-07-09 version-management update: F049 is now managed in `v0.1.30` together with the completed Partner foundation batch. This file remains the historical design source.
> 2026-07-12 rebaseline: this file mixes historical design and shipped artifact work. F048 is superseded by F056-F059, which are completed. F049 runs through the current artifact store plus the shipped Partner workbench (`F095`); generated office deliverables use `F109`. F055 is planned in [v0.1.32](v0.1.32.md) as packaged-origin and security hardening. Keep feature IDs stable; do not resurrect F048.
> 2026-06-08 Planning。Partner 批次第二版，对应 [ADR-007](../ADR/ADR-007-partner-surface-model.md) 三件套的**Artifact 层** + **Skill Packs 首杀**。Artifact 把 Partner 从"对话框"升级为"有产物的工作台"；近场 skill（代码相关知识工作）是 Partner 与 Coder 的天然交界、差异化最强、对现有用户最近的场景。**内核无关**——产物与 skill 都跑在现有 runtime + 既有能力上。

## 版本目标

让 Partner 能产出一个**可预览、可迭代、可导出**的 artifact（先吃文本/markdown 类，富格式渲染复用 F024），并落地第一组真正有价值的 skill：用 Repointel 把代码库变成架构文档 / API 文档 / PR 描述 / 评审摘要。DoD：在 Partner 里对一个代码仓库问"给这个模块写一份架构文档"，agent 产出一个 markdown artifact，右栏预览，可"再改一版"并导出到文件 / 贴进 PR。

## 本版本范围

| Feature | 描述 | Current plan | 关键文件 |
|--|--|--|--|
| F048 | Artifact 一等概念（登记 / 富预览 / 迭代版本 / 导出） | Superseded by F056-F059（Done） | `features/artifact/` + `electron/artifact/` + `ipc/artifact.ts` |
| F049 | Partner skill pack：代码相关知识工作（复用 Repointel） | Completed in `v0.1.30`; implemented as Workbench built-in code-knowledge routes using F095 + current artifact store, F109 generated office deliverables, and F113 reviewed file proposals | `renderer/src/features/partner/partnerWorkbench.ts` |
| F055 | 打包 renderer 迁 `app://` 自定义标准协议（artifact 嵌入安全地基） | Planned in `v0.1.32`; canonical design: [v0.1.32](v0.1.32.md) | `electron/main.ts`（scheme 注册 / protocol.handle / loadURL / CSP / navigation 白名单） |

依赖：F048 不再实现；F056-F059 是当前 artifact 数据层/生成/Panel UX 的 source of truth。F049 依赖 F047（工具集）+ F056-F059（产出落到 artifact store）+ F095（Workbench 交付流）；如果输出 docx/xlsx/pdf/pptx，则依赖 F109。
**F055 是 future sandbox/protocol hardening 的安全前置**——dev 不依赖它（dev renderer 已是 `http://localhost:5173`），但打包 renderer / future LiveCanvas React tier（F067）/ app-owned sandbox 需要真实父 origin `app://space`，不再归因到已废弃的 F048 路径 D。

---

## FEATURE_048: Artifact 一等概念

> Historical only. F048 is superseded by F056-F059: static artifact rendering baseline, data layer, `create_artifact` generation, and ArtifactPanel UX. Do not implement `artifact.write` from this old design.

### 需求概述

Partner 的产出（report / slides / sheet / doc / markdown）不能只是对话流里的一段文本，必须是**一等产物**：可在右栏富预览、带版本可"再改一版"、可导出到文件或外部（PR/Issue/Slack）。本 feature 立 artifact 的数据模型 + 右栏预览 + 迭代 + 导出；富格式渲染复用既有 `features/preview/`（F024）。

### 影响范围

- **renderer store**：新增 `store/artifacts.ts`——`ArtifactRef { id, sessionId, kind, title, path?, versions: ArtifactVersion[] }`，`kind: 'markdown'|'docx'|'pptx'|'xlsx'|'report'`。
- **renderer**：`features/partner/ArtifactPanel.tsx` 填充——列表 + 当前 artifact 富预览（复用 `features/preview/` 渲染器）+ 版本切换器 + 导出按钮。
- **main**：新增 `electron/ipc/artifact.ts`——`artifact.write`（写产物到作用域目录/临时区）、`artifact.export`（另存为 / 复制到剪贴板 / 贴外部占位）。
- **产出捕获**：agent 产出 artifact 的约定——优先复用 SDK 已有的 artifact 概念（`KodaXInputArtifact` / `listArtifacts` / `readArtifact` 在 SDK export 中存在），把 agent 写出的产物登记为 ArtifactRef；无 SDK 支持的 kind 走 main 侧文件落地。

### 技术方案

```ts
// store/artifacts.ts
interface ArtifactVersion { v: number; createdAt: string; path: string; summary?: string; }
interface ArtifactRef {
  id: string; sessionId: string;
  kind: 'markdown' | 'docx' | 'pptx' | 'xlsx' | 'report';
  title: string; current: number; versions: ArtifactVersion[];
}
```

- **登记**：监听 agent 产出事件——若 agent 经 write/skill 落了一个产物文件，main 发 `artifact.registered` 事件，renderer 入 store。优先消费 SDK 的 `listArtifacts`/`readArtifact`（若 managed run 暴露），否则按"产物写进作用域 + 约定路径"捕获。
- **迭代**："再改一版" = 带上当前 artifact 内容作为 context 发新指令，agent 产出新版本 → `versions.push`，`current++`，预览默认跳到最新，可回看旧版（复用 Monaco diff 比对两版）。
- **导出**：`artifact.export` → 另存为（原生 save dialog）/ 复制 markdown 到剪贴板 / "贴到 PR/Issue/Slack"（M2 Connector 前先占位为复制）。

### 接口契约

Historical API sketch only; current artifact APIs are F056-F059 plus F109 for generated office files.

```ts
// packages/space-ipc-schema/src/channels/artifact.ts
artifact.write   : { sessionId, kind, title, content } → { id, version, path }
artifact.list    : { sessionId } → { artifacts: ArtifactRef[] }
artifact.read    : { id, version? } → { content, kind }
artifact.export  : { id, version?, target: 'file'|'clipboard' } → { ok, path? }
```

安全：写产物受 F047 作用域约束（path inside scope）；导出"file"走原生 dialog（用户选路径，不静默写任意位置）。

### 实现步骤

1. schema `channels/artifact.ts` + index 注册。
2. `store/artifacts.ts` slice。
3. `electron/ipc/artifact.ts`：write/list/read/export handler；先支持 markdown + 透传 F024 可渲染格式。
4. 产出捕获：探测 SDK `listArtifacts`/`readArtifact` 在 managed run 是否可用；可用则消费，否则文件约定捕获（design 标注二选一的实测点）。
5. `ArtifactPanel.tsx`：列表 + 预览（接 `features/preview/`）+ 版本切换 + 导出。
6. 迭代链路：把"再改一版"接到对话输入（注入当前版本内容为 context）。
7. 测试：write→list→read→export 闭环；版本递增 + 旧版可读；越界写被拒。

### 验收标准

- [ ] agent 产出一份 markdown，右栏 ArtifactPanel 出现条目并富预览。
- [ ] "再改一版"产生 v2，预览跳最新，可切回 v1，两版可 diff。
- [ ] 导出"另存为"弹原生 dialog 写到用户选定路径；"复制"把 markdown 进剪贴板。
- [ ] docx/xlsx/pdf 类 artifact 复用 F024 渲染器预览（不另造渲染）。
- [ ] 产物写入受 F047 作用域约束，越界被拒。
- [ ] design 标注"SDK artifact API vs 文件约定捕获"的实测选择点。

---

## FEATURE_049: Partner skill pack：代码相关知识工作（复用 Repointel）

> Status: Completed (2026-07-09)

### 需求概述

Partner 的**首杀场景**——代码相关、但非编码的知识工作：架构文档、API 文档、changelog、PR 描述、需求拆解、评审摘要。这是 Partner 与 Coder 的天然交界，复用 Repointel 仓库智能，差异化最强、对现有开发者用户最近。本 feature 落地 3–4 个起步 skill，产出走当前 artifact store（F056-F059）；需要 office 文件时走 F109。

### 影响范围

- 新 `electron/skill/partner-packs/code-knowledge/`：skill 定义（复用 KodaX skill 机制 + `@kodax-ai` skills API）。起步 skill：
  - `draft-architecture-doc`——读模块 + Repointel overview/impact → 架构文档 artifact
  - `draft-pr-description`——读 working-tree diff（复用 F044 git diff）→ PR 描述 artifact
  - `summarize-review`——读 diff + 评审上下文 → 评审摘要 artifact
  - `extract-requirements`——读 issue/需求文本 → 结构化需求拆解 artifact
- 复用：Repointel 调用经 KodaX 内核已暴露的 status/overview/impact（[PRD §4.3](../PRD.md)：Space 只调已暴露接口，不碰 Repointel 内部对象）。

### 技术方案

每个 skill = 一个 KodaX skill（progressive disclosure），声明：触发描述、所需工具（read/grep/glob + repo intelligence 只读）、产出 = 一个 artifact。skill 在 Partner 工具白名单（F047）内运行。产出统一通过 `create_artifact` / artifact store（F056-F059）；office 文件输出通过 F109 writer，不要求 Partner `write`/`bash`。

> 不新增执行语义——skill 是 KodaX 既有机制，Space 只是把这组非编码 skill 组织进 Partner surface（[ADR-007](../ADR/ADR-007-partner-surface-model.md) 的"skill packs 组织成 surface"）。

### 接口契约

无新 IPC。skill 经现有 skill 加载/调用通道；产出经 `create_artifact` / artifact store。生成型 office 产物经 F109。

### 实现步骤

1. 建 `partner-packs/code-knowledge/` + 4 个 skill 定义（markdown skill 文件 + 触发描述）。
2. 接 Repointel 只读调用（overview/impact）到 skill 上下文。
3. 产出 → `create_artifact` / artifact store（kind=markdown/report；office deliverable 走 F109）。
4. Partner surface skill 列表 UI：左栏或输入区展示可用 skill（极简：自动按场景触发 > 显式选）。
5. 测试：对一个 fixture repo 跑 `draft-architecture-doc`，产出非空 artifact 且引用真实文件路径（faithfulness 占位校验——完整判官留 F053）。

### Implementation Progress

2026-07-09:

- Delivered the code-knowledge pack as built-in Partner Workbench routes instead of a separate runtime or new IPC channel.
- Added explicit task presets for API documentation and structured requirements breakdown, alongside the existing RFC, changelog, PR description, and review brief flows.
- Added an API documentation skill pack and code-knowledge route definitions for `draft-architecture-doc`, `draft-api-doc`, `draft-changelog`, `draft-pr-description`, `summarize-review`, and `extract-requirements`.
- Workbench prompts steer these tasks to read-only repo evidence: `read`/`grep`/`glob`, Partner KB search, and Repointel/repo-intelligence overview or impact tools when exposed by the active session.
- Generated deliverables continue through F109 `create_office_artifact`; workspace-visible markdown/text goes through F113 reviewed file proposals.
- Prompts explicitly prohibit raw `write`, `edit`, `multi_edit`, `bash`, `shell`, or unrestricted filesystem mutation.
- Added targeted pure tests for skill route resolution, default output paths, code-knowledge prompt steering, and safe output routing.

Completion notes:

- The original `electron/skill/partner-packs/code-knowledge/` directory sketch is intentionally superseded by Workbench-native skill routes: current Space already has a safe Partner composer/session path, and adding a second skill discovery surface would duplicate semantics.
- Repointel remains accessed only through exposed read-only repo-intelligence tools when available; this feature does not reach into Repointel internals.
- Full faithfulness judging for weak-evidence drafts remains with F053.

### 验收标准

- [x] Partner 里对一个代码仓库触发 `draft-architecture-doc`，产出架构文档 artifact，内容引用真实模块/文件。
- [x] `draft-pr-description` 基于 working-tree diff 产出 PR 描述。
- [x] 4 个 skill 都在 Partner 工具白名单内运行（无 bash）。
- [x] Repointel 调用只走已暴露接口，不触碰内部对象（符合 [PRD §4.3](../PRD.md)）。
- [x] 产出全部登记到 artifact store，可预览/迭代/导出；生成型 office 产物必须走 F109，不要求 Partner write/bash。
- [ ] 弱证据场景（agent 没读够文件就下笔）暂记 known-issue，待 F053 的 faithfulness 判官根治。

---

## FEATURE_055: 打包 renderer 迁 `app://` 自定义标准协议

> 2026-06-14 起。Artifact 路径 D（嵌 LiveCanvas sandbox，见记忆 `livecanvas_artifact_plan` / `livecanvas_gap_sandbox_bundle`）的**打包正式版安全地基**。源于与 LC 的集成确认：sandbox iframe 靠 `postMessage` 锁定一个**真实可比对的父窗口 origin**；打包后 renderer 走 `file://`（origin 为 `null`/不透明）无法锁定，只能 `"*"`（任何本机进程可冒充父窗口、隔离作废）——LC 明确此路无安全解。解法是把打包 renderer 迁到注册的 `app://` 标准安全协议，给它一个真实 origin `app://space`。**LC 侧据此放宽 `parent-origin.ts`/`child.ts` 的协议检查以接受受信列表中的 `app://`，并保留禁 `"*"`/null。**

### 需求概述

把**打包正式版**的 renderer 从 `file://`（`loadFile`）迁到 `protocol.registerSchemesAsPrivileged` 注册的 `app://` 标准安全协议，window 经 `loadURL('app://space/index.html')` 加载，主进程 `protocol.handle('app', …)` 从 `RENDERER_DIST` 提供文件。**dev 不变**（仍 `http://localhost:5173`，Vite HMR 需 http；LC localhost bypass 已覆盖）。这是 app 级（整窗口 origin）改动，非 artifact 局部，单列一个 feature。

### 影响范围（集中在 `electron/main.ts` 的"大门口"）

- **scheme 注册**：`app.whenReady()` 之前 `protocol.registerSchemesAsPrivileged([{ scheme:'app', privileges:{ standard:true, secure:true, supportFetchAPI:true, codeCache:true } }])`。
- **protocol handler**：`protocol.handle('app', …)` 把 `app://space/<path>` 映射到 `RENDERER_DIST` 内文件；path-traversal 安全（只 serve 目录内，拒越界），MIME 推断。
- **窗口加载**：`createMainWindow()` 的 prod 分支 `loadFile` → `loadURL('app://space/index.html')`；dev 分支不动。
- **CSP**（`applyCsp`）：prod `default-src 'self'` 对 `app://space` 自然生效；保留 `THEME_BOOTSTRAP_INLINE_HASH`、`worker-src 'self' blob:`（Monaco）、`style-src 'unsafe-inline'`；按需把 artifact sandbox 的 `frame-src`/`connect-src` 加进来（F067/F110 消费时）。
- **导航/外链**：`will-navigate` 的 `ALLOWED_FILE_PREFIX` 判定 → `app://space/` 前缀；`setWindowOpenHandler`、`will-navigate` 的 https 外链放行逻辑保持。
- **不受影响**：preload（仍按 `PRELOAD_PATH` 注入）、所有 IPC、terminal/mcp/notification/updater 等（它们不依赖 renderer 的 scheme）。

### 技术方案

```ts
// 1) app ready 之前（模块顶层或 whenReady 之前同步调）
protocol.registerSchemesAsPrivileged([
  { scheme: 'app', privileges: { standard: true, secure: true, supportFetchAPI: true, codeCache: true } },
]);

// 2) whenReady 内、createMainWindow 之前
const RENDERER_ORIGIN = 'app://space';
protocol.handle('app', (request) => {
  const { pathname } = new URL(request.url);          // app://space/index.html → /index.html
  const rel = decodeURIComponent(pathname).replace(/^\/+/, '') || 'index.html';
  const abs = safeJoinWithinRoot(RENDERER_DIST, rel); // 越界→null
  if (!abs) return new Response('forbidden', { status: 403 });
  // SPA/静态：缺失文件回退 index.html 视需要；MIME 由扩展名推断
  return netFileResponse(abs);                        // 复用 net.fetch(pathToFileURL) 或自读
});

// 3) prod 加载
mainWindow.loadURL(`${RENDERER_ORIGIN}/index.html`);
```

注意点：① Windows + asar 下 `RENDERER_DIST` 路径与现状一致（`__dirname/../apps/desktop/dist`）；handler 读 asar 内文件用 `net.fetch(pathToFileURL(abs))` 最稳（Electron 原生支持读 asar）。② `app://space` host 段必须合法且固定；renderer 内所有资源引用须 root-relative（Vite build base='./' 或 '/' 需核对，保证 `app://space/assets/...` 解析正确）。③ secure scheme 才是 secure context（crypto.subtle / SW 等可用），与 file:// 行为差异需回归。

### 接口契约

无新 IPC。导出常量 `RENDERER_ORIGIN = 'app://space'`（prod）作为 **artifact sandbox 注入的受信父 origin**（F067/F110：Space 自托管 sandbox 时把受信父 origin 注入 sandbox）。dev 下父 origin 取 `window.location.origin`（`http://localhost:5173`）走 dev bypass。

### 实现步骤

1. `registerSchemesAsPrivileged`（app ready 前）。
2. `protocol.handle('app', …)` + path-safety helper + asar 友好读取，单测覆盖正常/越界/MIME。
3. prod `loadURL('app://space/index.html')`；核对 Vite build 的 asset base，保证资源 root-relative 解析。
4. 改 `applyCsp` / `will-navigate` / `ALLOWED_FILE_PREFIX` / windowOpenHandler 的 prod 分支到 `app://` 前缀。
5. **全链路回归**（关键）：启动可见、session/对话、内置终端、Monaco（worker blob:）、MCP、通知、自动更新、外链放行、剪贴板粘贴图片——逐项在打包 smoke 下验不回归。
6. e2e：打包构建 boot smoke 断言 `location.origin==='app://space'` + 现有 e2e 全过。

### 验收标准

- [ ] 打包版 renderer 从 `app://space/index.html` 加载，`window.location.origin === 'app://space'`。
- [ ] dev 模式仍 `http://localhost:5173`，HMR/DevTools 正常。
- [ ] `app://` handler 拒绝路径穿越（`../` 越界 → 403），MIME 正确。
- [ ] 现有全部功能在 `app://` 下无回归（session/terminal/mcp/monaco/通知/更新/外链/剪贴板）。
- [ ] CSP 不引入任何 `'unsafe-*'` 新放宽（除既有 Monaco blob: / theme hash / style inline）。
- [ ] 导出 `RENDERER_ORIGIN` 供 F067/F110 sandbox 作受信父 origin；与 LC 集成回复 Q1/Q4 一致。
- [ ] secure-context 行为差异（vs file://）已逐项回归，无功能因 origin 变更而坏。

---

## FEATURE_056: 静态 Artifact 基线（LC-free）+ tier 门控

> 2026-06-15 起。**发布解耦**：LiveCanvas（LC）是半成品，Space 接近可发布。把 artifact 能力拆成两层 tier —— **静态 tier（LC-free，发布基线）** + **交互 React tier（依赖 LC 的 sandbox，路径 D）**。静态 tier 用 Space 现成能力拼装（md/code/F024 富预览 + 新增 html/chart），**发布永不依赖 LC**；react tier 门控关闭，待 LC GA（发 npm + F055 app://）再开。决策见与用户 2026-06-15 讨论。

### 需求概述

绝大多数真实 artifact 场景（报告 / 文档 / 表格 / 图表 / 排版）是**静态可渲染**的，不需要 eval 沙箱。唯有"AI 写任意交互式 React 当场跑"才需 LC sandbox。故：静态 kinds 全部 LC-free，作发布基线；`react` kind 收进 tier 门控（`isReactArtifactEnabled()`，发布 = false）。

### 影响范围

- **renderer 新增** `features/artifact/`：
  - `artifactKind.ts` —— kind 枚举 + 门控 `isReactArtifactEnabled()`。
  - `ArtifactView.tsx` —— 按 kind 派发的渲染器注册表。
  - `renderers/`：`HtmlArtifact`（iframe `srcdoc` + `sandbox=""` 禁脚本，新）、`ChartArtifact`（recharts from JSON spec，新）、`SvgArtifact`/`ImageArtifact`（直渲）；markdown→复用 `messages/Markdown`，code→Monaco，pdf/docx/xlsx→复用 F024 `RichPreview`，react→门控 `SandboxFrame`（P1c）。
- **renderer 依赖**：新增 `recharts`（仅 renderer；chart spec 直渲，无 eval）。
- **现有 P1c（LC tier）**：`SandboxFrame` / `useSandboxInfo` 原样保留，被门控收口。

### 技术方案

```ts
type ArtifactKind = 'markdown'|'code'|'html'|'svg'|'image'|'pdf'|'docx'|'xlsx'|'chart'|'react';
// 静态 kinds（LC-free）发布即可用；'react' 仅 isReactArtifactEnabled() 为真时启用。
```

- **html**：`<iframe srcdoc={html} sandbox="">`（空 sandbox = 禁脚本/表单/弹窗 → 纯静态安全；无新依赖、无需 dompurify，因 sandbox 已禁 JS）。
- **chart**：zod 校验 `{ type:'line'|'bar'|'area', data:[], series:[{key,color?}], xKey }` spec → 映射成 recharts 元素（renderer 内直渲，无 iframe、无 eval）。spec→config 抽纯函数便于单测。
- **门控**：`isReactArtifactEnabled()` 默认 false（发布）；dev / 显式 flag 开。`react` kind 命中门控关 → 显示"交互预览暂不可用"或不向 agent 开放该 kind。

### 实现步骤

1. `artifactKind.ts`（kind 枚举 + 门控）。
2. 新渲染器 `HtmlArtifact` + `ChartArtifact`（+ chart spec zod + 纯 map 函数）。
3. `ArtifactView` 注册表 + 复用包装（markdown/code/doc）。
4. recharts 依赖；ArtifactPanel 接 `ArtifactView`。
5. 验证：HTML 沙箱禁脚本（Playwright 独立 harness 真验）+ chart spec map（node:test 纯函数）+ typecheck/lint。
6. 后续层：artifact store + IPC + agent 生成（让基线真正产出/展示）。

### 验收标准

- [ ] 静态 kinds（markdown/code/html/svg/image/pdf/docx/xlsx/chart）全部 LC-free 渲染，无 `@livecanvas/*` / `@kodax-ai/livecanvas-sandbox-shell` import。
- [ ] HTML artifact 的 `sandbox=""` iframe 确认禁脚本（注入 `<script>` 不执行）。
- [ ] chart spec → recharts 渲染出 svg；非法 spec 安全降级（不崩）。
- [ ] `react` tier 默认门控关闭：发布版不暴露交互 React artifact。
- [ ] 复用 F024 RichPreview 渲染 pdf/docx/xlsx artifact，不另造。
- [ ] 发布构建不因 LC 缺失而坏（artifact 静态能力完整可用）。

---

## Artifact Roadmap（2026-06-15 定，SOT=记忆 livecanvas_artifact_plan）

双 tier:**静态 tier(LC-free,发布基线)** + **交互 React tier(LC sandbox,门控待 LC GA)**。发布与 LC 彻底脱钩(已实证:发布构建零 LC 代码/依赖)。F048 收敛为"M1 综合",原旧文本预览设计作废。

- **M1（静态可发布 MVP,优先)**:F056 渲染基线(done)→ **F057 数据层** → **F058 生成** → **F059 Panel UX+迭代导出**。
- **M2（LC 成熟后增强)**:F055(renderer app://)+ LC 发 npm → 打开 react tier 门控 + v1b AI-powered artifact。

## FEATURE_057: Artifact 数据层

### 需求概述
Space 自存 artifact(零 native;路径 D 决策),不依赖 LC/SDK 存储。提供 store + IPC,供 F058 生成写入、F059 UX 读取。

### 影响范围 / 技术方案
- `electron/artifact/store.ts` `ArtifactStore`:镜像 ProjectStore(内存缓存 + write-lock mutate + 原子写 tmp→rename/copyFile + schema 校验加载)。持久化 `~/.kodax/space/artifacts.json`。content 内联按 UTF-8 字节封顶(1MB),doc kind 只存 `path` 引用;`list` 返 content-stripped 元数据,`read` 取版本内容。`upsert` 返 `created`,未知 id→新建(fresh id,不复活已删)。MAX 1000/100/1MB 兜底。
- schema `channels/artifact.ts`:`artifactKind` 枚举 + `ArtifactRef` + `create/list/read/delete` + `artifact.changed` push。create 输入:字节封顶 + path 拒控制符 + kind↔content/path 一致性。
- `ipc/artifact.ts`:CRUD handler + 变更推 `artifact.changed`。

### 验收标准
- [x] store CRUD/版本/过滤/持久化/损坏恢复/并发/字节封顶 单测(14)。
- [x] IPC create/list/read/delete + changed push;preload allowlist 自派生。
- [x] 零 LC 依赖;过 code+security review(0 CRITICAL/HIGH)。

## FEATURE_058: Artifact 生成

### 需求概述
agent 如何产出 artifact —— **in-process `create_artifact` 工具**(走 SDK `registerTool`,同 F050 路子),agent 对"成形产物"自动 emit;`artifactId` 省=新建、带=新版本(迭代)。steering 优先静态 kind(react 仅门控开时开放)。

### 技术方案
main 注册 `create_artifact({kind,title,content?|spec?|path?,artifactId?})` → 调 `artifactStore.upsert`(同单例,不经 IPC)→ push `artifact.changed`。工具描述引导触发时机 + 限定 kind 集(按 `isReactArtifactEnabled` 之服务端等价策略)。产出受 F047 作用域约束。

### 验收标准
- [ ] agent 在对话中产出 markdown/chart artifact,自动登记进 store + 右栏出现。
- [ ] 迭代:带 artifactId 再 emit → 新版本。
- [ ] react kind 仅门控开放时可用(服务端策略,非仅前端)。

## FEATURE_059: ArtifactPanel UX + 迭代导出

### 需求概述
ArtifactPanel = 列表(当前 session)+ 当前项经 **ArtifactView**(F056)渲染 + 版本切换 + 动作条(迭代/导出)。Partner 右栏常驻;Coder popout。

### 技术方案
- renderer store slice(订阅 `artifact.changed` → 重拉 `artifact.list`)。
- 列表 + 版本切换器(读 `artifact.read(id,v)`)+ ArtifactView 渲染(ArtifactRef.kind→ArtifactContent 映射:content 类直传、doc 类传 {projectRoot,path} 给 RichPreview、chart 类 JSON.parse content)。
- "再改一版"=带当前版本内容作 context 发新指令(→ F058 工具再 emit 同 id)。
- 导出:另存为(原生 dialog)/ 复制(md/code/svg)/ 在编辑器打开;旧版 Monaco diff。

### 验收标准
- [ ] 列表/版本切换/渲染/空态;Partner 右栏 + Coder popout。
- [ ] "再改一版"→ 新版本,预览跳最新,可切回旧版 + diff。
- [ ] 导出另存为/复制可用;产出受作用域约束。
