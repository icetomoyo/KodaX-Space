# v0.1.43 - Beta Hardening + Sandbox Feasibility

> Design batch: F094 (+ bundled F069, F076, F077, F078)
> Status: Planned
> Source of truth: this file + [../FEATURE_LIST.md](../FEATURE_LIST.md)
> History: this was the `v0.1.40` beta-hardening lane in the F081-F094 batch ([v0.1.20.md](v0.1.20.md)); the 2026-06-29 F103 slip shifted it one feature slot to `v0.1.43`. IDs and design text are unchanged.

---

## Batch Intent

This is the last 0.1.x feature lane before the v0.2.x Partner/desktop-native expansion. It makes the product coherent for beta users: consistent version/docs/diagnostics, a packaged-app smoke checklist, an execution-sandbox feasibility checkpoint, and the i18n/logging foundation that the rest of beta builds on.

### Bundle members and canonical design

Two features in this lane keep their canonical, detailed design in their own dedicated docs — this file does not duplicate them:

| Feature | Theme | Canonical design |
| --- | --- | --- |
| F069 | Electron main-process unified Logger (levels + structured + rotating files + secret redaction + replace `console.*` + `no-console` lint) | [unplanned.md#F069](unplanned.md#f069--electron-主进程统一日志logger工程) |
| F076 | UI Language Switch and i18n Foundation (full typed locale, beyond the F104 Display Language MVP) | [v0.1.18.md#feature_076-ui-language-switch-and-i18n-foundation](v0.1.18.md#feature_076-ui-language-switch-and-i18n-foundation) |
| F077 | dev-only pseudo-locale / string-expansion QA | [v0.1.18.md#后续-feature](v0.1.18.md#后续-feature) |
| F078 | i18n scanner promoted from advisory to CI-required | [v0.1.18.md#后续-feature](v0.1.18.md#后续-feature) |

F076/F077/F078 build on the same locale primitives shipped by F104 (Display Language MVP, [v0.1.20.md#feature_104-display-language-mvp](v0.1.20.md#feature_104-display-language-mvp)) instead of replacing them. F094 (below) either integrates F069's logger or records an explicit dependency on it.

---

## FEATURE_094: Beta Quality, Release Cohesion, and Sandbox Feasibility

### 需求概述

Space has shipped quickly, but docs, version labels, release notes, logging, and diagnostics can drift. Beta users need a coherent product state, a repeatable packaged-app smoke checklist, and an explicit position on execution sandboxing (either supported with a path, or unsupported with rationale).

### 影响范围

- README / package / docs version alignment and release channel labels.
- About / Diagnostics export surface.
- Provider config security verification (custom `apiKeyEnv` / `baseUrl` guards introduced after `v0.1.19`).
- Depends on or integrates F069 logger.
- Produces a sandbox feasibility / threat-model checkpoint (implementation belongs to F099 or a later sandbox feature, not here).

### 技术方案

Create a beta readiness pass:

- README/package/docs version alignment,
- release channel labels,
- known capability degradation list,
- app diagnostics export,
- link to KodaX capability ledger,
- provider config security verification for custom `apiKeyEnv` and `baseUrl` guards introduced after `v0.1.19`,
- execution sandbox feasibility note: current trust boundary, platform options, minimum viable worktree sandbox, and explicit non-goals,
- F069 logger integration or explicit dependency,
- smoke checklist for packaged app boot, session open, provider config, permission prompt, workflow panel, artifact panel, Partner tab.

### 接口契约

- Reads `space.version` capability state and links to [KODAX_CAPABILITY_LEDGER.md](../KODAX_CAPABILITY_LEDGER.md).
- No new SDK contract required; consumes existing diagnostics/version IPC.

### 实现步骤

_待 Plan 阶段细化（sandbox feasibility output feeds F099 / a later sandbox feature）._

### 验收标准

- Package version, README status, and planning docs agree.
- About/Diagnostics exposes Space version, KodaX version, platform, and enabled capabilities.
- A packaged build has a repeatable smoke checklist.
- Known gaps are documented instead of implied by stale docs.
- Sandbox support is either explicitly marked unsupported with rationale or linked to a concrete `v0.2.x` implementation path.
- Provider guard tests for env injection and SSRF remain green before cutting the beta build.
