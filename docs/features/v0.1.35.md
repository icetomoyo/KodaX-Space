# v0.1.35 - Partner Write Path

> Design batch: F113
> Status: Completed
> Source of truth: this file + [../FEATURE_LIST.md](../FEATURE_LIST.md)
> 2026-07-09 version-management update: F113 is completed and now managed in the consolidated `v0.1.30` Partner foundation batch. This file remains the historical write-path design source.
> History: F103 was removed from this lane on 2026-07-06 and moved to `v0.1.29` after the Codex task-sidebar/right-sidebar and floating-surface design review. F113 moved here from the `v0.2.0` Partner re-enable foundation because Partner needs controlled workspace file writes before the broader `v0.2.0` workbench/artifact gate.
> Patch reservation: `v0.1.35` is now a patch reserve after the `v0.1.30` Partner foundation consolidation; `v0.1.36` remains the next feature-bearing workflow lane.

---

## Batch Intent

This version pulls the Partner write path forward: before Partner is fully re-enabled, Space should already have a controlled way for Partner to create or update ordinary workspace files without opening raw `write`, `edit`, or `bash`.

`F109` owns generated office deliverables in the artifact store. `F113` owns ordinary workspace file proposals and explicit apply/export. Together they let Partner produce useful user-visible output while keeping Partner off unrestricted shell and filesystem tools.

---

## FEATURE_113: Partner Controlled Workspace File Writes

### Problem

`F109` lets Partner generate finished office artifacts, but it does not let Partner create or update ordinary workspace files. That leaves a real usability hole: Partner can draft a PRD, changelog, meeting note, research memo, JSON/CSV output, or README update, but cannot save it into the user's selected project/folder without falling back to raw `write`/`edit` tools. Raw project mutation is intentionally blocked by `F047`, so the missing feature is a Space-owned reviewed file-write path, not a blanket permission unlock.

### Design

Add controlled workspace file writes as a Partner-safe Space tool and UI flow:

- introduce a narrow `create_file_proposal` / `update_file_proposal` tool family for Partner,
- proposals contain target path, operation kind, content or patch, source refs, rationale, content hash, and safety classification,
- target paths must resolve inside the current Partner source/output scope and pass traversal, symlink, hidden-dir, binary, size, and overwrite checks,
- proposals land in a review queue with diff preview, source/citation metadata, and apply / reject / save-as actions,
- applying a proposal is performed by Space after explicit user confirmation or a future policy rule, not by exposing raw Partner `write`/`edit`,
- support new text-like files first: `md`, `txt`, `json`, `yaml`, `csv`, `html`, `css`, and other safe text formats; existing-file updates use diff review,
- code file edits are allowed only through the same review/apply path and should offer "handoff to Coder" when the task becomes coding-heavy,
- successful apply creates an audit/provenance entry and refreshes file/diff/artifact surfaces where relevant.

This feature complements `F109`: `F109` owns generated office deliverables in the artifact store; `F113` owns ordinary workspace file proposals and explicit apply/export. Both keep Partner off raw shell and unrestricted filesystem tools.

### Acceptance

- Partner can propose a new markdown/text/json/csv/html file and the user can apply it to a selected workspace/output folder.
- Partner can propose an edit to an existing text file and Space shows a diff before apply.
- Invalid paths, path traversal, hidden-system targets, symlink escapes, oversized files, and binary edits are blocked with clear reasons.
- Applying a proposal updates the workspace through Space, records audit/provenance metadata, and refreshes the relevant file/diff view.
- Rejecting a proposal leaves the workspace unchanged and keeps the proposal visible in session history.
- Partner still cannot call raw `write`, `edit`, `multi_edit`, `bash`, or shell-backed tools in the Partner surface.

### Verification

- `npm run typecheck`
- `npm run build:smoke`
- targeted tests for Partner file proposal validation, diff review, apply/reject, and blocked raw write/edit/bash behavior

### Implementation Progress

2026-07-09:

- Added shared IPC schemas for `partner.fileProposals.list/get/apply/reject`.
- Added a persistent Partner file proposal store under Space-owned state.
- Added Partner-safe `create_file_proposal` and `update_file_proposal` SDK tools.
- Registered the tools through the existing Partner tool policy as scoped `mutates-state` tools, without allowing raw `write`, `edit`, `multi_edit`, `bash`, or shell-backed tools.
- Implemented guarded apply/reject behavior with explicit content-hash confirmation, stale-update detection, diff-cache refresh, path traversal checks, hidden-path blocking, binary/office/media rejection, size caps, and symlink-parent escape protection.
- Added a Partner right-rail review queue with Pending/All filters, proposal detail, safety/source metadata, visible diff preview, and explicit Apply / Reject / Save as actions.
- Added `partner.fileProposals.export` for native save-as export of proposal content with the same content-hash guard.
- Apply, reject, and save-as actions now append local session notices so the decision remains visible in the Partner conversation/history.
- Added targeted schema, store, tool, and Partner policy regression tests.

Completion notes:

- The proposal store is the F113 provenance record: status, hashes, rationale, source refs, timestamps, reject reason, and apply/export decisions are preserved locally.
- `F098` can later add policy/audit export over this record without expanding Partner's raw filesystem permissions.
- Verification passed with `npm run test -w @kodax-space/space-ipc-schema -- --test-reporter=spec`, targeted Partner file proposal/tool/policy tests, `npm run typecheck` (`NODE_OPTIONS=--max-old-space-size=4096`), and `npm run build:smoke`.

### Later Hooks

- `F095` consumes this as the reviewed workspace-file output path for Partner workbench tasks.
- `F098` can add policy controls for proposal/apply defaults, allowed roots, file types, and audit export.
- Connector-backed research and GitHub issue/PR loops can later attach source refs to file proposals without expanding Partner's raw write permissions.
