# KodaX Space v0.1.34 Release Readiness

> Status: Published; local gates, final pre-tag GitHub `main`, four-platform
> preflight, tagged release workflow, public Release, and assets verified
>
> Published tag: [`v0.1.34`](https://github.com/icetomoyo/KodaX-Space/releases/tag/v0.1.34)
>
> Release date: 2026-07-30
>
> Approved candidate commit:
> `f31a897e981374bae555aa8a7a7066a1b0f16a39`
>
> Tagged release commit:
> `e6b7081a139966896b888e8e415aa9f5a8858e3f`
>
> Space package version: `0.1.34`
>
> KodaX dependency: exact npm Registry `@kodax-ai/kodax@0.7.78`
>
> Release design: [v0.1.34 Runtime safety and desktop lifecycle hardening](../features/v0.1.34.md)

## 1. Release decision

`v0.1.34` is the verified stabilization release after published `v0.1.33`. It
ships:

- explicit KodaX 0.7.78 Runtime safety/capability negotiation;
- last-known-good MCP/A2A/Extension configuration and visible integration
  health;
- Auto guardrail v4 retry and one-call fallback semantics;
- structured active-tool sandbox/fallback facts;
- physical packaged ASRT/sandbox helper resolution;
- cross-platform complete exit and Space-scoped orphan-idle recovery;
- one main-owned startup/shutdown overlay;
- exact positional history replay after completed interrupt runs; and
- updated README, user/developer manuals, PRD, HLD, capability ledger,
  Feature List, Known Issues, changelog, and `kodax_manual`.

This release does **not** claim the F137/F139 native document/semantic UI lane.
That reviewed design moved intact to `v0.1.36`.

## 2. Version and dependency matrix

| Source                      | Required                | Published         |
| --------------------------- | ----------------------- | ----------------- |
| root `package.json`         | `0.1.34`                | `0.1.34`          |
| Desktop workspace           | `0.1.34`                | `0.1.34`          |
| IPC schema workspace        | `0.1.34`                | `0.1.34`          |
| UI kit workspace            | `0.1.34`                | `0.1.34`          |
| Runtime capability contract | `space-v0.1.34`         | `space-v0.1.34`   |
| root KodaX dependency       | exact `0.7.78`          | exact `0.7.78`    |
| Desktop KodaX dependency    | exact `0.7.78`          | exact `0.7.78`    |
| lockfile KodaX package      | Registry `0.7.78`       | Registry `0.7.78` |
| installed KodaX package     | Registry `0.7.78`       | Registry `0.7.78` |
| changelog section           | `[0.1.34] - 2026-07-30` | present           |

Registry integrity:

- SHA-256: `34860911…5CA43BF5`
- lockfile SRI:
  `sha512-D33K2cSFM6Xyi1x8Q2Bwjv6KEGzZIdIlqSN+Odt9MVWgoEb0TCARlaC98ds+M1S+jlhCay+8masnStzbxk6Itg==`

## 3. Scope truth audit

- [x] `docs/features/v0.1.34.md` describes only implemented 0.1.34 work.
- [x] F137/F139 and their complete design moved to `v0.1.36`.
- [x] F118 remains in `v0.1.35`; later roadmap targets are unchanged.
- [x] README and README_CN identify v0.1.34 as the current published release.
- [x] User manual documents integration health, Auto v4, sandbox labels,
      cross-platform exit, orphan recovery, and safe legacy daemon cleanup.
- [x] `kodax_manual` gives the same current answers while preserving every
      installed SDK underlying-capability topic.
- [x] HLD, PRD, capability ledger, Feature List, Builtin Skills, Known Issues,
      and developer usage guide agree on ownership and limitations.
- [x] CHANGELOG contains Added/Changed/Fixed/Verification sections.
- [x] Historical v0.1.32/v0.1.33 records retain their release facts and carry a
      post-release note for the F137/F139 schedule change.

## 4. Required Runtime and package gates

- [x] Both Runtime-consuming manifests pin exact Registry KodaX 0.7.78.
- [x] `npm ls @kodax-ai/kodax --all` resolves one deduplicated package.
- [x] Runtime compatibility probe requires `daemonOrphanExit:1`,
      `integrationConfigResilience:1`, `runtimeAutoModeGuardrail:4`,
      `skillLearningLoop:1`, and the established shared-session safety contracts.
- [x] Attachment is capability-negotiated instead of rejected by a second
      semantic-version floor.
- [x] Package smoke verifies public facades, ancestor-resolved dependency
      closure, native SQLite, physical ASRT/helper paths, and sandbox doctor.
- [x] `kodax_manual` regression preserves the SDK topic body/alias/source
      contract and asserts the 0.1.34 Runtime/integration/sandbox guidance.

## 5. Local automated evidence

| Gate                                           | Status | Evidence                                                                                 |
| ---------------------------------------------- | ------ | ---------------------------------------------------------------------------------------- |
| `npm run typecheck`                            | ✅     | Passed 2026-07-30 on Node 22.23.1                                                        |
| `npm test`                                     | ✅     | 21 release tests; 1,973 passed + 4 skipped of 1,977 Desktop tests; 287 IPC-schema tests  |
| `npm run lint`                                 | ✅     | Passed with zero warnings                                                                |
| release-file Prettier check                    | ✅     | Every changed and added release file passed                                              |
| `npm run build:smoke`                          | ✅     | Built skills, workspaces, production renderer, preload, worker, and main process         |
| focused 0.1.34 suites                          | ✅     | 224/224 Runtime, exit, overlay, history, MCP, slash, and `kodax_manual` tests            |
| Windows E2E                                    | ✅     | 74 passed, 2 environment skips, 0 failures                                               |
| Windows package + `smoke:pack` + packaged boot | ✅     | Setup/Portable generated; package smoke and KodaX 0.7.78 Runtime/renderer boot both pass |

The initial full test run restored `better-sqlite3` from the Electron ABI to
the Node 22 ABI before executing the Desktop suite. That expected rebuild log
is not a test failure.

The repository-wide `npm run format:check` still reports 304 pre-existing
formatting mismatches, mostly historical documentation and vendored builtin
Skill assets outside this release. Reformatting that entire baseline would
mix hundreds of unrelated mechanical changes into the release. The release
therefore formats and checks every changed/added release file while keeping
the full-repository baseline explicit.

The first Windows E2E pass found one stale test assumption: Playwright exposes
the new boot `WebContentsView` as a short-lived Page, so the tray-reopen test
raced that overlay instead of selecting the `app://space/` BrowserWindow. The
fixture now selects the main renderer by URL and waits for atomic overlay
removal. The focused scenario passed once, then 3/3 repeated runs, followed by
the full 74-pass suite.

The first GitHub E2E candidate exposed a second early-page race in the dedicated
startup-overlay spec: its `onLaunched` hook evaluated the main process while
Playwright was still replacing the boot `WebContentsView` execution context.
The assertion now runs only after the fixture selects `app://space/`, atomically
checks second-instance activation, and holds both overlay phases for a
deterministic window. The scenario passed 3/3 locally and then passed in both
GitHub Windows/Linux shard 2 jobs.

The macOS packaged-daemon preflight also exposed Darwin's 104-byte Unix-socket
path limit. KodaX 0.7.78 correctly requires auto-start to use its profile
default endpoint, so Space temporarily normalizes Darwin `TMPDIR` to `/tmp`
only while the SDK computes/spawns that endpoint and restores the host
environment afterward. Both macOS arm64 and Intel packaged Runtime lifecycle
smokes pass with this compatibility layer.

Local Windows artifact evidence:

| Artifact                          |       Bytes | SHA-256                                                            |
| --------------------------------- | ----------: | ------------------------------------------------------------------ |
| `KodaX-Space-Setup-0.1.34.exe`    | 169,513,955 | `A42A7BE026592DD6A0F0C515C491325C51247243649F48F9BBCD4A225D9A4209` |
| `KodaX-Space-Portable-0.1.34.exe` | 169,304,055 | `A57E15E6C943D032BEEC31A98468EED4233920E31957B7D7D5DA28055BC6CFC8` |

## 6. GitHub `main` evidence

Approved candidate:
`f31a897e981374bae555aa8a7a7066a1b0f16a39`.

| Workflow          | Required result                                                 | Candidate run                                                                                                                                                   |
| ----------------- | --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `build`           | Windows/macOS/Linux smoke jobs all green                        | **Passed** — [`30480333986`](https://github.com/icetomoyo/KodaX-Space/actions/runs/30480333986)                                                                 |
| `ci`              | Windows/Linux E2E shards 1/2 and 2/2 all green                  | **Passed** — [`30480335215`](https://github.com/icetomoyo/KodaX-Space/actions/runs/30480335215); all four shards succeeded                                      |
| release preflight | Windows, macOS x64/arm64, and Linux installers/smokes all green | **Passed** — [`30480332722`](https://github.com/icetomoyo/KodaX-Space/actions/runs/30480332722); Release creation intentionally skipped for `workflow_dispatch` |

The pre-candidate `ad38c87` runs were red because the commit upgraded the
dependency to KodaX 0.7.78 while the Runtime compatibility tests still asserted
0.7.77 and the old Auto v3 missing-model behavior. The 0.1.34 candidate updates
those probes to the published 0.7.78 contracts. Those failed run IDs are
historical evidence, not release approval:

- `build`: `30469154801`
- `ci`: `30469151334`

Release-preflight history retained for diagnosis:

- `30476233378`: both macOS package jobs exposed an opaque packaged-daemon
  startup failure; Space added redacted bootstrap-tail diagnostics.
- `30478086190`: the diagnostics identified `listen EINVAL` on the overlong
  Darwin default Unix socket.
- `30479450307`: confirmed that a custom endpoint is not a valid workaround
  because KodaX auto-start requires the profile default endpoint.
- `30480332722`: final preflight passed on Windows, Ubuntu, macOS arm64, and
  macOS Intel using the bounded default-endpoint environment.

The final evidence commit and tag target,
`e6b7081a139966896b888e8e415aa9f5a8858e3f`, also passed the required GitHub
workflows before the tag was created:

| Workflow | Final pre-tag run                                                                                                          |
| -------- | -------------------------------------------------------------------------------------------------------------------------- |
| `build`  | **Passed** — [`30481687300`](https://github.com/icetomoyo/KodaX-Space/actions/runs/30481687300)                            |
| `ci`     | **Passed** — [`30481687364`](https://github.com/icetomoyo/KodaX-Space/actions/runs/30481687364); all four shards succeeded |

## 7. Tag and release workflow evidence

| Gate                       | Required result                                             | Result                                                                                                                                            |
| -------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| manual release preflight   | all four platform build/smoke jobs pass                     | **Passed** — [`30480332722`](https://github.com/icetomoyo/KodaX-Space/actions/runs/30480332722)                                                   |
| annotated tag              | `v0.1.34` points at the approved final documentation commit | **Passed** — `e6b7081a139966896b888e8e415aa9f5a8858e3f`                                                                                           |
| release build: Windows     | Setup/Portable/zip + smoke                                  | **Passed** — [`30482823053`](https://github.com/icetomoyo/KodaX-Space/actions/runs/30482823053)                                                   |
| release build: macOS x64   | DMG + smoke                                                 | **Passed** — [`30482823053`](https://github.com/icetomoyo/KodaX-Space/actions/runs/30482823053)                                                   |
| release build: macOS arm64 | DMG + smoke                                                 | **Passed** — [`30482823053`](https://github.com/icetomoyo/KodaX-Space/actions/runs/30482823053)                                                   |
| release build: Linux       | AppImage/deb + package smoke + renderer boot smoke          | **Passed** — [`30482823053`](https://github.com/icetomoyo/KodaX-Space/actions/runs/30482823053)                                                   |
| GitHub Release             | stable, not prerelease; notes sourced from CHANGELOG        | **Passed** — [`v0.1.34`](https://github.com/icetomoyo/KodaX-Space/releases/tag/v0.1.34) is public, stable, and uses the 0.1.34 CHANGELOG notes    |
| release assets             | installers, updater metadata, and published digests         | **Passed** — 18 expected assets; updater YAML contains SHA-512 metadata and GitHub publishes SHA-256 digests; this release is explicitly unsigned |

Primary published artifact evidence:

| Artifact                             |       Bytes | GitHub SHA-256                                                     |
| ------------------------------------ | ----------: | ------------------------------------------------------------------ |
| `KodaX-Space-Setup-0.1.34.exe`       | 169,514,202 | `3f800d4940a24ed4097bc53fe18137fb43703717e790136d620335fb714fbe01` |
| `KodaX-Space-Portable-0.1.34.exe`    | 169,304,303 | `d7444b31d83b847e8651eab7d6401a5878c232b2d4d87016e52242ace63ecfdb` |
| `KodaX-Space-0.1.34-x64.dmg`         | 202,371,567 | `f17e637b4f5cac043a7dcf13da0f29bcea3b37c9a11b350d12d0d4e5066ac3da` |
| `KodaX-Space-0.1.34-arm64.dmg`       | 199,535,539 | `dc5a1127161abcfeef0bc9920a1b64b028a609c4bae4e1730b90c4379121ff7c` |
| `KodaX-Space-0.1.34-x86_64.AppImage` | 207,739,800 | `299ead73451031fe2a092f183c31e91780daca9cc813db0c4de89175825ba823` |
| `KodaX-Space-0.1.34-amd64.deb`       | 166,973,036 | `09e7f0adaf320b8078cfb62cbec8548fd2948e7304e09c8934f6185ae3545310` |

The other 12 expected assets are the Windows/macOS ZIPs, blockmaps, and
`latest.yml`/`latest-mac.yml`/`latest-linux.yml` updater metadata. There is no
separate signature manifest: code signing and notarization were not configured
for this release.

## 8. Manual and known-risk record

Automated release approval does not erase these explicit boundaries:

- F121 still requires its separately recorded final human multi-client
  acceptance ledger.
- Issue 133 remains `In Progress` for packaged macOS/Linux process-level exit
  acceptance and KodaX 0.7.78's asynchronous orphan-cleanup
  retry/verification gap.
- `daemonOrphanExit:1` proves that the current host enabled orphan idle-exit; it
  does not expose a pre-existing daemon's effective grace interval or guarantee
  retry after every asynchronous cleanup failure.
- KodaX command containment and packaged helpers do not complete F138's
  document staging, credential, native-resource, and cross-platform OS
  isolation.
- Unsigned macOS/keychain behavior remains tracked separately; publication
  does not imply code signing or notarization.

These are documented limitations, not hidden substitutions. The required
release workflow completed without a failed job.

## 9. Completed publication sequence

The following sequence completed in order on 2026-07-30:

1. Complete code, version, documentation, `kodax_manual`, and Known Issues.
2. Run local typecheck, lint, format, tests, focused suites, smoke build, E2E,
   and Windows packaging/smoke.
3. Commit and push `main`.
4. Wait for the candidate commit's GitHub `build` and `ci` workflows to become
   fully green.
5. Record the exact commit and workflow run evidence here; push that
   documentation update and wait for its workflows to become fully green.
6. Create and push annotated tag `v0.1.34`.
7. Wait for every release-workflow platform job and the final Release job.
8. Verify the public GitHub Release is stable, contains the CHANGELOG notes,
   and exposes all 18 expected assets and digests.
9. Update this record and README wording from candidate to published after that
   evidence exists.
